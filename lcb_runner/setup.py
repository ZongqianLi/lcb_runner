from setuptools import setup, find_packages

setup(
    name="lcb_runner",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # 在这里列出你的依赖包
        # 例如: "numpy", "pandas", 等
    ],
)